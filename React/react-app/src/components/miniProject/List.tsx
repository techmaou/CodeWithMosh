const List = () => {
  return (
    <div className="mb-3">
      <label htmlFor="select" className="form-label">
        All Categories
      </label>
      <select id="select" className="form-select"></select>
    </div>
  );
};

export default List;
